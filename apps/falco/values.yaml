driver:
  kind: modern_ebpf

falcosidekick:
  enabled: true
  config:
    loki:
      hostport: http://loki.monitoring.svc.cluster.local:3100
      minimumpriority: notice
  webui:
    enabled: true 
    service:
      type: LoadBalancer
